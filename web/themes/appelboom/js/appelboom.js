/**
 * @file
 * Appelboom behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.appelboom = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
